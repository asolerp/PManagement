# ========================================
# Android Signing Configuration
# ========================================
# IMPORTANTE: Este archivo contiene información sensible
# NO lo subas a Git (ya está en .gitignore)

# Nombre del archivo keystore (debe estar en android/app/)
ANDROID_KEYSTORE_PATH=portmanagement.keystore

# Password del keystore (el que usaste al generar el keystore)
ANDROID_KEYSTORE_PASSWORD=CAMBIA_ESTO_POR_TU_PASSWORD

# Alias de la key dentro del keystore
ANDROID_KEY_ALIAS=portmanagement

# Password de la key (puede ser el mismo que el del keystore)
ANDROID_KEY_PASSWORD=CAMBIA_ESTO_POR_TU_PASSWORD

# ========================================
# INSTRUCCIONES:
# ========================================
# 1. Si ya tienes un keystore de producción:
#    - Cópialo a: android/app/portmanagement.keystore
#    - Reemplaza los passwords arriba con los reales
#
# 2. Si NO tienes keystore, genera uno con:
#    keytool -genkeypair -v -storetype PKCS12 \
#      -keystore android/app/portmanagement.keystore \
#      -alias portmanagement \
#      -keyalg RSA \
#      -keysize 2048 \
#      -validity 10000
#
# 3. Guarda las contraseñas en un lugar seguro (1Password, LastPass, etc)
#
# 4. RENOMBRA este archivo a .env (sin el .local)
#    mv .env.local .env
#
# 5. Después de configurar:
#    npx expo prebuild --clean
#    cd android && ./gradlew bundleRelease
