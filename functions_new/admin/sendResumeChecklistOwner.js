/**
 * Envío de resumen de checklist al propietario por email.
 * Desactivado: comunicaciones por email eliminadas de momento.
 * notifyOwner sigue llamando a esta función; no hace nada.
 */
const sendResumeChecklistOwner = async () => {
  return;
};

module.exports = {
  sendResumeChecklistOwner,
};
